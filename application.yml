server:
  port: 8080

spring:
  application:
    # 应用的名称
    name: reggie_take_out
  datasource:
    # druid数据库连接
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: 123456
  # 邮箱配置
  mail:
    host: smtp.qq.com  # 发送邮件的服务器地址
    username: 1435045598@qq.com # 开启 IMAP/SMTP服务 的qq邮箱的账号
    password: lpmefnsqlfblhhba # 开启 IMAP/SMTP服务 获得的授权码,而不是qq邮箱的登录密码
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          ssl:
            enable: true

mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射 address_book ---> AddressBook
    map-underscore-to-camel-case: true
    #日志输出
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      #该配置完成后，每个模型类的主键ID策略都将成为assign_id
      id-type: ASSIGN_ID

#文件图片等保存路径，这样写方便修改
reggie:
  path: E:\reggie_img\

  spring:
    redis:
      # 加入redis相关配置
      host: localhost # 本地IP或者是虚拟机IP
      port: 6379
      # password: 1234 连接密码，没设置就关闭
      database: 0